---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: webservers.ldhctlr.linuxdatahub.com
spec:
  group: ldhctlr.linuxdatahub.com
  names:
    kind: Webserver
    listKind: WebserverList
    plural: webservers
    shortNames:
    - ws
    - wss
    singular: webserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Webserver is the Schema for the webservers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WebserverSpec defines the desired state of Webserver
            properties:
              image:
                type: string
              ingres:
                properties:
                  host:
                    type: string
                  path:
                    type: string
                required:
                - host
                - path
                type: object
              int:
                example: 3
                format: int32
                type: integer
              namespace:
                description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file'
                type: string
              servicetype:
                properties:
                  port:
                    description: kubebuilder:example=[default,foo,bar]
                    format: int32
                    type: integer
                  targerport:
                    format: int32
                    type: integer
                  type:
                    description: kubebuilder:example=[ClusterIP;NodePort;LoadBalancer]
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                required:
                - port
                - type
                type: object
            required:
            - image
            - ingres
            - int
            - namespace
            - servicetype
            type: object
          status:
            description: WebserverStatus defines the observed state of Webserver
            properties:
              deployment:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
              servicetype:
                type: string
              weburl:
                type: string
            required:
            - servicetype
            - weburl
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
